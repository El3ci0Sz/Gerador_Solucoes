1. Geração Padrão (Clock USE)
Gera 10 grafos em um grid 15x15, exigindo 20 portas lógicas. 

python scripts/runner.py single --tec qca --backwards --k-graphs 10 --arch-size 15 15 --num-gates 20 --visualize --output-dir datasets/results_use 

2. Geração com Clock 2DDWAVE  
Gera 10 grafos usando o esquema de clock direcional (T). 

python scripts/runner.py single --tec qca --qca-arch T --backwards --k-graphs 10 --arch-size 15 15 --num-gates 20 --visualize --output-dir datasets/results_2ddwave 
